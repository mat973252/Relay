/**
 * Single source of truth for the Relay version reported by doctor output.
 * Must stay in sync with packages/core/package.json "version" (enforced by test).
 */
export const RELAY_VERSION = "0.1.0";
